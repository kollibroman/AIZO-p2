#include "Menu/MainMenu.h"
#include "Tests/TestRandomGraphGeneration.h"

int main()
{
    MainMenu menu;
    menu.show();

    // auto test = new TestRandomGraphGeneration();
    //
    // int vertices, density;
    // std::cout << "Enter number of vertices: ";
    // std::cin >> vertices;
    // std::cout << "Enter density (0-100): ";
    // std::cin >> density;
    // test->generateRandom(vertices, density, false);
    return 0;
}
